# Hafnia SDK — datasets + experiment tracking
hafnia>=0.7.9

# RF-DETR is installed from the local ./rf-detr folder via the Dockerfile.
# Listing its runtime deps explicitly so `pip install -r requirements.txt` also works.
torch>=2.2.0,<2.11
torchvision>=0.17.0
# >=5.9.0 required for the DINOv3 ViT backbone (DINOv3ViTBackbone/DINOv3ViTConfig) used by the
# dinov3_* encoder path; 5.9.0 is the version verified locally (scripts/check_dinov3_warmstart.py).
transformers>=5.9.0,<6.0.0
pytorch_lightning>=2.6,<3,!=2.6.2,!=2.6.3
torchmetrics[detection]>=1.2
supervision>=0.20
pydantic>=2.0
pyDeprecate
peft
pycocotools
# >=2.0 for OpticalDistortion(mode="fisheye") used by the fisheye_night aug preset (verified on 2.0.8).
albumentations>=2.0.0
tensorboard
wandb

# YOLO11 comparison trainer (scripts/train_yolo.py)
ultralytics
